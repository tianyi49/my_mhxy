# 如需二次开发（开发说明）

请参考[MaaPracticeBoilerplate](https://github.com/MaaXYZ/MaaPracticeBoilerplate),对本项目进行完善\
本项目模板不是最新模板，

# MaaPracticeBoilerplate

MaaFramework 实践模板。

## 如何开发

0. 使用右上角 `Use this template` - `Create a new repository` 来基于本模板创建您自己的项目。

1. 完整克隆本项目及子项目（地址请修改为您基于本模板创建的新项目地址）。

    ```bash
    git clone --recursive https://github.com/gitlihang/Maa_MHXY_MG.git
    ```

    **请注意，一定要完整克隆子项目，不要漏了 `--recursive`**

2. 下载 MaaFramework 的 [Release 包](https://github.com/MaaXYZ/MaaFramework/releases)，解压到 `deps` 文件夹中。

3. 配置资源文件。

    ```bash
    python ./configure.py
    ```

4. 按需求修改 `assets` 中的资源文件，请参考 MaaFramework 相关文档。

    - 可使用 [MaaDebugger](https://github.com/MaaXYZ/MaaDebugger) 进行调试；
    - 也可以在本地安装后测试：

        1. 执行安装脚本
           
      ```bash
      python ./install.py
      ```
      
        2. 运行 `install/MaaPiCli.exe`

5. 完成开发工作后，上传您的代码并发布版本。

    ```bash
    # 配置 git 信息（仅第一次需要，后续不用再配置）
    git config user.name "您的 GitHub 昵称"
    git config user.email "您的 GitHub 邮箱"
    
    # 提交修改
    git add .
    git commit -m "XX 新功能"
    git push origin HEAD -u
    ```

6. 发版您的版本

    需要先修改仓库设置 `Settings` - `Actions` - `General` - `Read and write permissions` - `Save`
    
    ```bash
    # CI 检测到 tag 会自动进行发版
    git tag v1.8.3
    git push origin v1.8.3
    ```
    v2.11.7-beta.1
    
    git push origin HEAD
    
    git tag v2.16.0-beta.7
    git push origin v2.16.0-beta.7
    




fix:修复周长迷魂塔（公测）
fix:修复宝图任务结束识别
fix:修复三界奇缘加载题库失败

docs:更新mac教程文档2

feat:新增三界奇缘，文字识别易错字兼容-频
feat:新增全局默认参数onerror空节点
feat:新增三界奇缘答题正确率输出
feat:新增任务链（高级功能）

perf:优化框架新版adb路径变更，模拟器渲染模式检查出现问题
perf:优化科举乡试开始答题ai答题apikey传递
perf:修复宠物祈福-文字识昴日星官-文字识别错误问题
perf:兼容秘境跳转、运镖活动识别，老电脑反应慢问题
perf:更新三界奇缘题库26.5.22
perf:优化捉过任务等待

chore:升级阿瓦隆版本MFAA_VERSION: "v2.12.1"。3.升级棉絮MXU_VERSION: "v2.1.6"
chore:更新OCR子模块
chore:升级阿瓦隆版本MFAA_VERSION: "v2.12.1"
chore:升级棉絮MXU_VERSION: "v2.2.1"
chore:升级maafw版本5.11.0.升级棉絮MXU_VERSION: v2.1.6

test:测试ui版本v2.6.2
style:优化代码格式

{ message = "^feat", group = "<!-- 0 -->✨ 新功能" },
{ message = "^fix", group = "<!-- 1 -->🐛 Bug修复" },
{ message = "^docs", group = "<!-- 4 -->📚 文档" },
{ message = "^perf", group = "<!-- 3 -->🚀 性能优化" },
{ message = "^refactor", group = "<!-- 2 -->🚜 代码重构" },
{ message = "^style", group = "<!-- 5 -->🎨 样式" },
{ message = "^test", group = "<!-- 6 -->🧪 测试" },
{ message = "^chore\\(deps\\)", group = "📦 依赖更新" },
{ message = "^chore", group = "<!-- 8 -->🔧 日常维护" },
{ message = "^ci", group = "<!-- 7 -->⚙️ 持续集成" },
{ body = ".*security", group = "<!-- 9 -->🔒 安全" },
{ message = "^[Rr]evert", group = "<!-- 10 -->◀️ 撤销" }


