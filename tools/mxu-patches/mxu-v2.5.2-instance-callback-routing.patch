warning: in the working copy of 'src-tauri/Cargo.lock', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src-tauri/Cargo.toml', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src-tauri/src/commands/maa_agent.rs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src-tauri/src/commands/maa_core.rs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src-tauri/src/commands/types.rs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src-tauri/src/commands/utils.rs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src-tauri/src/lib.rs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src-tauri/src/web_server.rs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src-tauri/src/ws_broadcast.rs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src-tauri/tauri.conf.json', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/services/maaService.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/services/wsService.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/utils/useMaaCallbackLogger.ts', LF will be replaced by CRLF the next time Git touches it
diff --git a/src-tauri/Cargo.lock b/src-tauri/Cargo.lock
index 326ffe3..7e4cd86 100644
--- a/src-tauri/Cargo.lock
+++ b/src-tauri/Cargo.lock
@@ -3127,7 +3127,7 @@ dependencies = [

 [[package]]
 name = "mxu"
-version = "0.1.0"
+version = "2.5.2"
 dependencies = [
  "axum",
  "base64 0.22.1",
diff --git a/src-tauri/Cargo.toml b/src-tauri/Cargo.toml
index a1c34f0..9a2712f 100644
--- a/src-tauri/Cargo.toml
+++ b/src-tauri/Cargo.toml
@@ -1,6 +1,6 @@
 [package]
 name = "mxu"
-version = "0.1.0"
+version = "2.5.2"
 description = "A Tauri App"
 authors = ["you"]
 edition = "2021"
diff --git a/src-tauri/src/commands/maa_agent.rs b/src-tauri/src/commands/maa_agent.rs
index d276381..ce5a4c4 100644
--- a/src-tauri/src/commands/maa_agent.rs
+++ b/src-tauri/src/commands/maa_agent.rs
@@ -20,7 +20,9 @@ use maa_framework::resource::Resource;
 use maa_framework::tasker::Tasker;

 use super::types::{AgentConfig, ControllerInfo, MaaState, TaskConfig};
-use super::utils::{emit_callback_event, get_logs_dir, handle_task_callback, normalize_path};
+use super::utils::{
+    emit_instance_callback_event, get_logs_dir, handle_task_callback, normalize_path,
+};
 use regex::Regex;
 use std::sync::LazyLock;
 use std::time::{Duration, Instant};
@@ -585,7 +587,7 @@ pub async fn start_tasks_impl(
                     detail,
                 );
                 // 再转发原始回调到前端
-                emit_callback_event(&app_handle, msg, detail);
+                emit_instance_callback_event(&app_handle, Some(&inst_id_for_sink), msg, detail);
             })
             .map_err(|e| e.to_string())?;
             debug!("[start_tasks] Tasker sink added");
@@ -597,7 +599,7 @@ pub async fn start_tasks_impl(
             t.add_context_sink(move |msg, detail| {
                 // 遥测：把失败节点挂到任务 Span 下，形成失败链路
                 super::telemetry::on_node_event(&inst_id_for_ctx_sink, msg, detail);
-                emit_callback_event(&app_handle, msg, detail);
+                emit_instance_callback_event(&app_handle, Some(&inst_id_for_ctx_sink), msg, detail);
             })
             .map_err(|e| e.to_string())?;
             debug!("[start_tasks] Tasker context sink added");
diff --git a/src-tauri/src/commands/maa_core.rs b/src-tauri/src/commands/maa_core.rs
index be55ddb..1e2a8da 100644
--- a/src-tauri/src/commands/maa_core.rs
+++ b/src-tauri/src/commands/maa_core.rs
@@ -21,7 +21,7 @@ use super::types::{
     AdbDevice, ConnectionStatus, ControllerConfig, GamescopeInstance, MaaState, TaskStatus,
     VersionCheckResult, Win32Window,
 };
-use super::utils::{emit_callback_event, get_maafw_dir, handle_task_callback, normalize_path};
+use super::utils::{get_maafw_dir, handle_task_callback, normalize_path};

 /// MaaFramework 最小支持版本
 const MIN_MAAFW_VERSION: &str = "5.12.1";
@@ -1023,7 +1023,7 @@ pub async fn connect_controller_impl(
             }
         };

-        // 注册回调（使用 on_event 抽象，Tauri 命令传入 emit_callback_event，HTTP 处理器传入无操作或 WebSocket 推送）
+        // 注册回调（使用 on_event 抽象，Tauri 命令携带实例标识，HTTP 处理器通过 WebSocket 推送）
         let on_event_clone = on_event.clone();
         controller
             .add_sink(move |msg, detail| {
@@ -1146,7 +1146,17 @@ pub async fn maa_connect_controller(
         state.inner().clone(),
         instance_id.clone(),
         config,
-        Arc::new(move |msg, detail| emit_callback_event(&app, msg, detail)),
+        Arc::new({
+            let callback_instance_id = instance_id.clone();
+            move |msg, detail| {
+                super::utils::emit_instance_callback_event(
+                    &app,
+                    Some(&callback_instance_id),
+                    msg,
+                    detail,
+                )
+            }
+        }),
     )
     .await;
     if result.is_ok() {
@@ -1248,7 +1258,15 @@ pub fn maa_load_resource(
         &paths,
         Arc::new({
             let app = app.clone();
-            move |msg, detail| emit_callback_event(&app, msg, detail)
+            let callback_instance_id = instance_id.clone();
+            move |msg, detail| {
+                super::utils::emit_instance_callback_event(
+                    &app,
+                    Some(&callback_instance_id),
+                    msg,
+                    detail,
+                )
+            }
         }),
         Some(&app),
     )?;
@@ -1357,7 +1375,12 @@ pub fn run_task_impl(
                     msg,
                     detail,
                 );
-                emit_callback_event(&app_for_sink, msg, detail);
+                super::utils::emit_instance_callback_event(
+                    &app_for_sink,
+                    Some(&instance_id_for_sink),
+                    msg,
+                    detail,
+                );
             })
             .map_err(|e| e.to_string())?;

@@ -1367,7 +1390,12 @@ pub fn run_task_impl(
             .add_context_sink(move |msg, detail| {
                 // 遥测：把失败节点挂到任务 Span 下，形成失败链路
                 super::telemetry::on_node_event(&instance_id_for_context_sink, msg, detail);
-                emit_callback_event(&app_for_context_sink, msg, detail);
+                super::utils::emit_instance_callback_event(
+                    &app_for_context_sink,
+                    Some(&instance_id_for_context_sink),
+                    msg,
+                    detail,
+                );
             })
             .map_err(|e| e.to_string())?;

diff --git a/src-tauri/src/commands/types.rs b/src-tauri/src/commands/types.rs
index 1550421..dce7bd4 100644
--- a/src-tauri/src/commands/types.rs
+++ b/src-tauri/src/commands/types.rs
@@ -391,6 +391,7 @@ impl MaaState {
 /// Maa回调事件
 #[derive(Clone, Serialize, Deserialize)]
 pub struct MaaCallbackEvent {
+    pub instance_id: Option<String>,
     pub message: String,
     pub details: String,
 }
diff --git a/src-tauri/src/commands/utils.rs b/src-tauri/src/commands/utils.rs
index 84e0ab2..9a33b22 100644
--- a/src-tauri/src/commands/utils.rs
+++ b/src-tauri/src/commands/utils.rs
@@ -8,21 +8,32 @@ use std::path::PathBuf;
 use std::sync::Arc;
 use tauri::{AppHandle, Emitter, Manager};

-/// 发送回调事件到前端（Tauri WebView + WebSocket 浏览器客户端）
-pub fn emit_callback_event<S: Into<String>>(app: &AppHandle, message: S, details: S) {
+/// 发送带实例归属的回调，避免多实例运行时把日志写入当前活动标签页。
+pub fn emit_instance_callback_event<S: Into<String>>(
+    app: &AppHandle,
+    instance_id: Option<&str>,
+    message: S,
+    details: S,
+) {
     let message = message.into();
     let details = details.into();
+    let instance_id = instance_id.map(str::to_string);

     // 广播到所有 WebSocket 客户端
     if let Some(ws) = app.try_state::<Arc<WsBroadcast>>() {
         ws.send(WsEvent::MaaCallback {
+            instance_id: instance_id.clone(),
             message: message.clone(),
             details: details.clone(),
         });
     }

     // 发送到 Tauri WebView
-    let event = MaaCallbackEvent { message, details };
+    let event = MaaCallbackEvent {
+        instance_id,
+        message,
+        details,
+    };
     if let Err(e) = app.emit("maa-callback", event) {
         log::error!("Failed to emit maa-callback: {}", e);
     }
@@ -52,7 +63,7 @@ pub fn emit_state_changed(app: &AppHandle, instance_id: &str, kind: &str) {

 /// 处理 MaaFramework 任务回调，在 Rust 侧更新 TaskRunState（单一真相来源）
 ///
-/// 应在 tasker sink 中调用，在 `emit_callback_event` 之前处理任务状态变更。
+/// 应在 tasker sink 中调用，在发送回调事件之前处理任务状态变更。
 /// 负责跟踪 Tasker.Task.Starting / Succeeded / Failed，并在所有任务完成后
 /// 更新 `overall_status` 并发射 `tasks-completed` 事件。
 pub fn handle_task_callback(
diff --git a/src-tauri/src/lib.rs b/src-tauri/src/lib.rs
index f5db780..c341e2f 100644
--- a/src-tauri/src/lib.rs
+++ b/src-tauri/src/lib.rs
@@ -63,7 +63,7 @@ pub fn run() {
             // 创建 AppConfigState：加载 interface.json 和配置文件
             let app_config = Arc::new(AppConfigState::default());

-            // 创建 WebSocket 广播器（容量 256，被 emit_callback_event/emit_agent_output 共用）
+            // 创建 WebSocket 广播器（容量 256，被 Maa 回调事件与 agent 输出共用）
             let ws_broadcast = Arc::new(WsBroadcast::new(256));

             // 加载 interface.json（含 import 处理和翻译）
diff --git a/src-tauri/src/web_server.rs b/src-tauri/src/web_server.rs
index e5c9dc8..c6af64a 100644
--- a/src-tauri/src/web_server.rs
+++ b/src-tauri/src/web_server.rs
@@ -31,7 +31,7 @@ use crate::commands::{
         post_screencap_impl, run_task_impl, stop_task_impl,
     },
     types::{AgentConfig, ControllerConfig, ControllerInfo, MaaState, TaskConfig},
-    utils::{emit_callback_event, emit_config_changed, emit_state_changed},
+    utils::{emit_config_changed, emit_state_changed},
 };
 use crate::ws_broadcast::WsBroadcast;

@@ -818,8 +818,14 @@ async fn handle_connect_controller(
     ensure_instance_exists(&state.maa_state, &instance_id);

     let app_handle = state.app_handle.clone();
+    let callback_instance_id = instance_id.clone();
     let on_event = Arc::new(move |msg: &str, detail: &str| {
-        emit_callback_event(&app_handle, msg, detail);
+        crate::commands::utils::emit_instance_callback_event(
+            &app_handle,
+            Some(&callback_instance_id),
+            msg,
+            detail,
+        );
     });

     match connect_controller_impl(state.maa_state, instance_id.clone(), config, on_event).await {
@@ -860,9 +866,15 @@ async fn handle_load_resource(
     };

     let app_handle = state.app_handle.clone();
+    let callback_instance_id = instance_id.clone();
     let on_event: Arc<dyn Fn(&str, &str) + Send + Sync + 'static> =
         Arc::new(move |msg: &str, detail: &str| {
-            emit_callback_event(&app_handle, msg, detail);
+            crate::commands::utils::emit_instance_callback_event(
+                &app_handle,
+                Some(&callback_instance_id),
+                msg,
+                detail,
+            );
         });

     match load_resource_impl(
diff --git a/src-tauri/src/ws_broadcast.rs b/src-tauri/src/ws_broadcast.rs
index 7613d4f..67b66bb 100644
--- a/src-tauri/src/ws_broadcast.rs
+++ b/src-tauri/src/ws_broadcast.rs
@@ -14,7 +14,11 @@ use tokio::sync::broadcast;
 pub enum WsEvent {
     /// Maa 框架回调（对应 Tauri `maa-callback` 事件）
     #[serde(rename = "maa-callback")]
-    MaaCallback { message: String, details: String },
+    MaaCallback {
+        instance_id: Option<String>,
+        message: String,
+        details: String,
+    },

     /// Agent 子进程输出（对应 Tauri `maa-agent-output` 事件）
     #[serde(rename = "maa-agent-output")]
diff --git a/src-tauri/tauri.conf.json b/src-tauri/tauri.conf.json
index 9be7a3d..f232533 100644
--- a/src-tauri/tauri.conf.json
+++ b/src-tauri/tauri.conf.json
@@ -1,7 +1,7 @@
 {
   "$schema": "https://schema.tauri.app/config/2",
   "productName": "mxu",
-  "version": "0.1.0",
+  "version": "2.5.2",
   "identifier": "com.misteo.mxu",
   "build": {
     "beforeDevCommand": "pnpm dev",
diff --git a/src/services/maaService.ts b/src/services/maaService.ts
index cf1e9ad..c189edd 100644
--- a/src/services/maaService.ts
+++ b/src/services/maaService.ts
@@ -81,6 +81,8 @@ async function syncMaaVersionToStore(version: string): Promise<void> {

 /** MaaFramework 回调事件载荷 */
 export interface MaaCallbackEvent {
+  /** 产生该回调的实例；旧后端可能不提供 */
+  instance_id?: string;
   /** 消息类型，如 "Resource.Loading.Succeeded", "Controller.Action.Succeeded", "Tasker.Task.Succeeded" */
   message: string;
   /** 详细数据 JSON 字符串 */
@@ -726,30 +728,34 @@ export const maaService = {
    * - Node.Action.Starting/Succeeded/Failed - 节点动作状态
    */
   async onCallback(
-    callback: (message: string, details: MaaCallbackDetails) => void,
+    callback: (
+      message: string,
+      details: MaaCallbackDetails,
+      instanceId?: string,
+    ) => void,
   ): Promise<UnlistenFn> {
     if (!isTauri()) {
       // 浏览器环境：通过 WebSocket 接收 maa-callback 事件
-      return wsService.onMaaCallback((message, details) => {
+      return wsService.onMaaCallback((instanceId, message, details) => {
         try {
           const parsedDetails = JSON.parse(details) as MaaCallbackDetails;
-          callback(message, parsedDetails);
+          callback(message, parsedDetails, instanceId);
         } catch {
           log.warn('Failed to parse WS callback details:', details);
-          callback(message, {});
+          callback(message, {}, instanceId);
         }
       });
     }

     return await listen<MaaCallbackEvent>('maa-callback', (event) => {
-      const { message, details } = event.payload;
+      const { instance_id, message, details } = event.payload;

       try {
         const parsedDetails = JSON.parse(details) as MaaCallbackDetails;
-        callback(message, parsedDetails);
+        callback(message, parsedDetails, instance_id);
       } catch {
         log.warn('Failed to parse callback details:', details);
-        callback(message, {});
+        callback(message, {}, instance_id);
       }
     });
   },
diff --git a/src/services/wsService.ts b/src/services/wsService.ts
index 2fece42..9e8362d 100644
--- a/src/services/wsService.ts
+++ b/src/services/wsService.ts
@@ -15,6 +15,7 @@ const log = createLogger('wsService');
 // ============================================================================

 export interface WsMaaCallbackPayload {
+  instance_id?: string;
   message: string;
   details: string;
 }
@@ -35,7 +36,7 @@ export type WsMessage =
 // 订阅者类型
 // ============================================================================

-type MaaCallbackHandler = (message: string, details: string) => void;
+type MaaCallbackHandler = (instanceId: string | undefined, message: string, details: string) => void;
 type AgentOutputHandler = (instanceId: string, stream: string, line: string) => void;
 type ConfigChangedHandler = () => void;
 type StateChangedHandler = (instanceId: string, kind: string) => void;
@@ -168,7 +169,9 @@ function onMessage(event: MessageEvent) {

   switch (msg.type) {
     case 'maa-callback':
-      maaCallbackHandlers.forEach((h) => h(msg.payload.message, msg.payload.details));
+      maaCallbackHandlers.forEach((h) =>
+        h(msg.payload.instance_id, msg.payload.message, msg.payload.details),
+      );
       break;
     case 'maa-agent-output':
       agentOutputHandlers.forEach((h) =>
diff --git a/src/utils/useMaaCallbackLogger.ts b/src/utils/useMaaCallbackLogger.ts
index d7a33db..fde9b79 100644
--- a/src/utils/useMaaCallbackLogger.ts
+++ b/src/utils/useMaaCallbackLogger.ts
@@ -233,17 +233,18 @@ export function useMaaCallbackLogger() {
     // 设置回调监听
     const setupListener = async () => {
       try {
-        const unlisten = await maaService.onCallback((message, details) => {
+        const unlisten = await maaService.onCallback((message, details, callbackInstanceId) => {
           // 组件已卸载则忽略
           if (cancelled) return;

-          // 获取当前活动实例 ID
-          const currentActiveId = useAppStore.getState().activeInstanceId;
-          if (!currentActiveId) return;
+          // 新后端会携带准确实例；仅对旧后端回退到当前活动实例。
+          const targetInstanceId =
+            callbackInstanceId || useAppStore.getState().activeInstanceId;
+          if (!targetInstanceId) return;

           // 根据消息类型处理
           handleCallback(
-            currentActiveId,
+            targetInstanceId,
             message,
             details as MaaCallbackDetails & Record<string, unknown>,
             t,
